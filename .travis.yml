dist: jammy
group: edge

jobs:
  include:
    - arch: arm64
    - arch: ppc64le      
    - arch: s390x
    # - arch: amd64
    
# group: edge
language: minimal

# go:
# - master


script:
  - apt-get update
  - cat /etc/hosts
